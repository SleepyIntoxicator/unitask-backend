package apiserver

import (
	"fmt"
	"testing"
)

func TestSendStdoutHandler(t *testing.T) {
	//testStore := teststore.New()
	//cfg := config.NewConfig()
	//srv := newServer(testStore, cfg)

	fmt.Println("TESTING STRING FOR sendStdoutHandler")
	//srv.sendStdout()
}
